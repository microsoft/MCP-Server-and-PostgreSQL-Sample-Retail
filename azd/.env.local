# Azure Developer CLI - Local Environment Variables
# This file provides default values for local development
# These will be overridden by azd-managed environment variables during deployment

# Azure Environment (will be set by azd)
# AZURE_ENV_NAME=dev
# AZURE_LOCATION=westus2
# AZURE_SUBSCRIPTION_ID=your-subscription-id

# Azure AI Services (will be populated by azd from infrastructure outputs)
# AZURE_OPENAI_ENDPOINT=https://your-ai-service.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o-mini
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-small

# Application Insights (will be populated by azd from infrastructure outputs)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key;IngestionEndpoint=https://your-region.in.applicationinsights.azure.com/

# PostgreSQL Database (for future integration)
# These can be customized for local development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=zava
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_APPLICATION_NAME=mcp-server

# Container Apps (will be populated by azd)
# AZURE_CONTAINER_APP_ENVIRONMENT_ID=/subscriptions/your-sub/resourceGroups/rg-your-env/providers/Microsoft.App/managedEnvironments/cae-your-env
# AZURE_CONTAINER_APP_ENVIRONMENT_NAME=cae-your-env

# Application Configuration
PORT=8000
AZURE_EXTENSION_USE_DYNAMIC_INSTALL=yes_without_prompt

# Service Principal (optional, for CI/CD scenarios)
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id

# Development Settings
DEBUG=true
LOG_LEVEL=INFO